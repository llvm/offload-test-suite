if 'Clang' in config.available_features:
    config.unsupported = True
# Disable these tests under metal until we've resolved issues with cbuffer
# binding access.
if 'Metal' in config.available_features:
    config.unsupported = True
